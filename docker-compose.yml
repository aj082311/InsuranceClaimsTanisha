services:
  insurance-claims-api:
    build:
      context: .
      dockerfile: Dockerfile
    image: insurance-claims-api:latest
    container_name: insurance-claims-api
    ports:
      - "8080:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
    restart: unless-stopped
